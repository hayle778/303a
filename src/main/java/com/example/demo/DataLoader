import com.example.demo.Component;
import com.example.demo.Flight;
import com.example.demo.FlightRepository;
import org.graalvm.compiler.lir.CompositeValue;
import org.springframework.beans.factory.annotation.Autowired;


@Component
public class DataLoader implemnets CommandLineRunner {
            @Autowired
            FlightRepository repository;
    @Override
    public void run(String... strings)  throws Exception {
        Flight Flight;
        flight = new Flight("Astrophysics", "Neil D Tyson", "Just a course on stars", 3);
        repository.save(course);
        flight = new Flight("Calculus", "Carol Henely",
                "Rate of Change of the Rate of Change", 3);
        repository.save(flight);
        flight = new Flight("Freshman English", "Geraldine pegram",
                "Learn your languge childern", 3);
        repository.save(flight);
    }
        }